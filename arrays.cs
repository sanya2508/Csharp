using System;

namespace ConsoleApp1
{
    class Program
    {
        static void Main(string[] args)
        {
            //Array
            int[] luckyNumbers = { 1, 3, 7, 11 };
            Console.WriteLine(luckyNumbers[2]);
            luckyNumbers[0] = 21;

            // we need to tell the number of elements
            string[] friends = new string[100];
            friends[0] = Apples;
            friends[1] = Car;


            Console.ReadLine();  
        }
    }
}
